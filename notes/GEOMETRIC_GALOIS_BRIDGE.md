# Geometric-Galois Bridge

This note records the mainstream bridge for the canonical-lane package.

Mainstream precursors:
- Frobenius action on etale cohomology and the cycle-class map give the standard arithmetic-geometric input.
- Galois-invariant classes and algebraic cycles provide the ordinary identification layer a reviewer expects.
- the lane adds admissible transport, explicit coherence ledgers, and a named remainder sector for classes not yet certified by the admissible carrier.

Problem-side translation:
- `P_mc` projects the visible object to the admissible sector `Y_mc^TATE` tracked by the lane.
- `Q_rem := I - P_mc` records the explicit remainder `R_TATE` carried by the defect and coherence ledgers.
- the manuscript closes on `Y_mc^TATE`; any stronger external consequence must control `R_TATE` rather than dropping it.

Reviewer use:
- translate the transport language into a decomposition between the admissible sector and the unresolved complement,
- compare the bridge package to the standard arithmetic/geometric objects rather than reading the lane as disconnected notation.
